
hardware:
    look into ht1622 pin for data ctrl
    find XCSOURCE XTM35SC manual for rs485 integration (modbus?)

node-red:
    find/write driver for rs485 (kmt)

agua:
    agua -> domain

    sensory history page
    only record sensor data while pump running
    manual pump activation has 1hr timer

    power meters
        (kmt read worked when properly terminated)
        pymodbus w/ tx/rx/sleep in subclass
        disabled bluetooth w/ modbus libs
        modbus w/ kmt
        all 3 modbus (with or without kmt)

        data_collector writing vals to db


laptop:
    swap fn/ctrl keys?

