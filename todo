

access logins:
	msg (fb/etc), github

cfg devsetup:
	vimrc, bashrc, pti?
    pi as gitserver


go back & read bookmarked pi automations

create fn for valves

created web: 
	view
	ctrl
	scheduler, etc
